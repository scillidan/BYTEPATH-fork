name: CI

on:
  push:
    branches: [main, dev]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: lunarmodules/luacheck@v1
        with:
          args: --codes --ranges .

  build-windows:
    runs-on: windows-latest
    needs: lint
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      matrix:
        include:
          - branch: master
            love_version: "0.10.2"
            suffix: love0.10.2
          - branch: dev
            love_version: "11.5"
            suffix: love11.5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ matrix.branch }}
      - name: Package .love
        run: |
          mkdir dist
          Compress-Archive -Path conf.lua,main.lua,globals.lua,utils.lua,GameObject.lua,tree.lua,libraries,objects,rooms,resources -DestinationPath dist\bytepath.zip
          Rename-Item dist\bytepath.zip bytepath.love
        shell: pwsh
      - name: Download LÖVE
        run: |
          $v = "${{ matrix.love_version }}"
          Invoke-WebRequest -Uri "https://github.com/love2d/love/releases/download/$v/love-$v-win64.zip" -OutFile "love-win64.zip"
          Expand-Archive -Path "love-win64.zip" -DestinationPath "love-win64"
      - name: Build Windows exe
        run: |
          $v = "${{ matrix.love_version }}"
          Copy-Item "love-win64\love-$v-win64\*" -Destination "dist\" -Recurse
          $loveExe = "dist\love.exe"
          $loveFile = "dist\bytepath.love"
          $outputExe = "dist\bytepath.exe"
          cmd /c "copy /b `"$loveExe`" + `"$loveFile`" `"$outputExe`""
          Remove-Item "dist\love.exe" -ErrorAction SilentlyContinue
          Remove-Item "dist\lovec.exe" -ErrorAction SilentlyContinue
          Remove-Item "dist\bytepath.love" -ErrorAction SilentlyContinue
          Compress-Archive -Path "dist\*" -DestinationPath "bytepath-windows-${{ matrix.suffix }}.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: bytepath-windows-${{ matrix.suffix }}
          path: bytepath-windows-${{ matrix.suffix }}.zip

  build-linux:
    runs-on: ubuntu-latest
    needs: lint
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      matrix:
        include:
          - branch: master
            love_version: "0.10.2"
            suffix: love0.10.2
            format: love
          - branch: dev
            love_version: "11.5"
            suffix: love11.5
            format: appimage
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ matrix.branch }}
      - name: Package .love
        run: |
          mkdir -p dist
          zip -r dist/bytepath.love conf.lua main.lua globals.lua utils.lua GameObject.lua tree.lua libraries objects rooms resources -x "*.git*"
      - name: Build Linux .love bundle (0.10.2)
        if: matrix.format == 'love'
        run: |
          cd dist
          echo '#!/bin/sh' > bytepath.sh
          echo 'if ! command -v love &> /dev/null; then echo "LÖVE 0.10.2 is required: https://love2d.org"; exit 1; fi' >> bytepath.sh
          echo 'SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"' >> bytepath.sh
          echo 'love "$SCRIPT_DIR/bytepath.love"' >> bytepath.sh
          chmod +x bytepath.sh
          zip ../bytepath-linux-${{ matrix.suffix }}.zip bytepath.love bytepath.sh
      - name: Build Linux AppImage (11.5)
        if: matrix.format == 'appimage'
        run: |
          wget -q "https://github.com/love2d/love/releases/download/11.5/love-11.5-x86_64.AppImage" -O love.AppImage
          chmod +x love.AppImage
          cd dist
          cat ../love.AppImage bytepath.love > bytepath-love11.5-x86_64.AppImage
          chmod +x bytepath-love11.5-x86_64.AppImage
          rm bytepath.love
      - uses: actions/upload-artifact@v4
        if: matrix.format == 'love'
        with:
          name: bytepath-linux-${{ matrix.suffix }}
          path: bytepath-linux-love0.10.2.zip
      - uses: actions/upload-artifact@v4
        if: matrix.format == 'appimage'
        with:
          name: bytepath-linux-${{ matrix.suffix }}
          path: dist/bytepath-love11.5-x86_64.AppImage

  build-linux-arm:
    runs-on: ubuntu-latest
    needs: lint
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
      - name: Package .love for ARM
        run: |
          mkdir -p dist
          zip -r dist/bytepath.love conf.lua main.lua globals.lua utils.lua GameObject.lua tree.lua libraries objects rooms resources -x "*.git*"
      - name: Build ARM .love bundle
        run: |
          cd dist
          echo '#!/bin/sh' > bytepath-gpi.sh
          echo 'if ! command -v love &> /dev/null; then echo "LÖVE 11.5 is required: https://love2d.org"; exit 1; fi' >> bytepath-gpi.sh
          echo 'SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"' >> bytepath-gpi.sh
          echo 'love "$SCRIPT_DIR/bytepath.love"' >> bytepath-gpi.sh
          chmod +x bytepath-gpi.sh
          zip ../bytepath-gpi.love-gpi-case-2.zip bytepath.love bytepath-gpi.sh
      - uses: actions/upload-artifact@v4
        with:
          name: bytepath-gpi-love-gpi-case-2
          path: bytepath-gpi.love-gpi-case-2.zip

  release:
    runs-on: ubuntu-latest
    needs: [build-windows, build-linux, build-linux-arm]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*
